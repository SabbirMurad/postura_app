import 'package:flutter/material.dart';

void printLine(dynamic item) {
  debugPrint('<==========================>');
  debugPrint('');
  debugPrint(item?.toString());
  debugPrint('');
  debugPrint('<==========================>');
}
